@page
@model DetailsModel
@{
    ViewData["Title"] = "Details";
}

<div class="text-center">
    <h1 class="display-4">Details</h1>
</div>

@if (Model.MessageForAuthenticatedUser is not null)
{
    <div
        class="text-body">
        <h4>@Model.MessageForAuthenticatedUser</h4>
    </div>   
}

@if (Model.MessageForAuthenticatedUserWithReaderRole is not null)
{
    <div
        class="text-body">
        <h4>@Model.MessageForAuthenticatedUser</h4>
    </div>   
}

@if (Model.MessageForAuthenticatedUserWithOwnerRole is not null)
{
    <div
        class="text-body">
        <h4>@Model.MessageForAuthenticatedUser</h4>
    </div>   
}

@if (Model.MessageForAuthenticatedUserWithContributorRole is not null)
{
    <div
        class="text-body">
        <h4>@Model.MessageForAuthenticatedUser</h4>
    </div>   
}